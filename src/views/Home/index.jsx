import Home from './Home';

import { connect } from 'react-redux';
// import {} from 'src/redux/actions/EditorActionCreators';

const mapStateToProps = (state) => ({});

export default connect(mapStateToProps, {})(Home);

